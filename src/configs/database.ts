const DATABASE_NAME = '@doit';

//@nomeapp:collection <-- Exemplo

const POST_IT_LIST = `${DATABASE_NAME}:postItList`;
const THEME_SAVED = `${DATABASE_NAME}:themeSaved`;

export {
  POST_IT_LIST,
  THEME_SAVED
}